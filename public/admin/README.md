# Decap CMS Admin

This folder will host the Decap CMS single‑page app and configuration:

- `index.html` – CMS bootstrap
- `config.yml` – CMS collections and backend

Filled in later per “Decap CMS skeleton” tasks in `todo.md`.


